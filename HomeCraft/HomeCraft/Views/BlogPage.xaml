<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="HomeCraft.Views.BlogPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:fonts="clr-namespace:HomeCraft.Models"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    xmlns:prism="http://prismlibrary.com"
    xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
    ios:Page.UseSafeArea="true"
    prism:ViewModelLocator.AutowireViewModel="True"
    Shell.NavBarIsVisible="False">
    <Grid
        Padding="15,10,15,10"
        BackgroundColor="{StaticResource GridBGColor}"
        RowDefinitions="30,*">
        <StackLayout
            Grid.Row="0"
            HorizontalOptions="End"
            Orientation="Horizontal"
            Spacing="10">
            <Label
                FontFamily="IconFont"
                FontSize="20"
                HorizontalOptions="Start"
                Text="{x:Static fonts:IconFont.Basket}"
                TextColor="Black"
                VerticalOptions="Center" />

            <Label
                FontFamily="IconFont"
                FontSize="25"
                HorizontalOptions="End"
                Text="{x:Static fonts:IconFont.Menu}"
                TextColor="Black"
                VerticalOptions="Center">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding ShellFlyoutCommmand}" />
                </Label.GestureRecognizers>
            </Label>
        </StackLayout>
        <Grid
            Grid.Row="1"
            RowDefinitions="50,2*,50,*"
            RowSpacing="20">
            <Label
                Grid.Row="0"
                HorizontalOptions="Start"
                Style="{StaticResource DarkSemiBoldLabel}"
                Text="Blog"
                VerticalOptions="Start" />


            <CollectionView
                Grid.Row="1"
                ItemTemplate="{StaticResource BlogPageMainBanner}"
                ItemsLayout="HorizontalList"
                ItemsSource="{Binding DesignStyless}" />


            <Grid Grid.Row="2">
                <Label
                    HorizontalOptions="Start"
                    Style="{StaticResource PRofilePageLabel}"
                    Text="Simple Lifestyle" />
                <Label
                    HorizontalOptions="End"
                    Style="{StaticResource PRofilePageLabel}"
                    Text="SEE ALL" />
            </Grid>

            <CollectionView
                Grid.Row="3"
                HeightRequest="300"
                ItemTemplate="{StaticResource BlogPageLightBanner}"
                ItemsLayout="HorizontalList"
                ItemsSource="{Binding DesignStyless}"
                VerticalOptions="FillAndExpand" />
        </Grid>
    </Grid>
</ContentPage>